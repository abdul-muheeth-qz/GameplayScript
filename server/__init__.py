"""The web API that joins the three stages together.

    python -m server              # http://127.0.0.1:8000

Serves the built UI from ui/dist when it exists; while developing, run `npm run dev`
in ui/ instead and let Vite proxy /api here.
"""
