captured_files/
__pycache__/
*.pyc

# One environment for the whole app (server/.venv), and it is not committed.
.venv/
venv/
env/
ENV/

# Left behind by running the OCR step against loose images.
roi_crops/

# The standalone validator stage 3 was ported from, kept on disk as reference. It is a
# git repository of its own, so committing it here would nest one inside the other.
spin-validator/

# UI
ui/node_modules/
ui/dist/
ui/.vite/
npm-debug.log*
